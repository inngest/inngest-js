---
"inngest": minor
---

Add `secrets` to sandbox create options to map environment variable names to workspace secret IDs. Values are fetched at launch and inherited by commands, managed processes, and snapshots.
