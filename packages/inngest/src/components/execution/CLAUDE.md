# execution/

Read `ARCHITECTURE.md` before making non-trivial changes in this folder. It covers concepts (e.g. lazy ops) that aren't obvious from the code alone.

Feature docs:

- `defer.md` — `createDefer` / `defer(...)` user-facing API and behavior.
