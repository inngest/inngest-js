import { createV1InngestExecution } from "./v1.js";

/**
 * V2 is functionally the same as V1, only Executor-side behaviour is different.
 */
export const createV2InngestExecution = createV1InngestExecution;
