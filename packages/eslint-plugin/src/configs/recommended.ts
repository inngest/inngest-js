export const recommended = {
  rules: {
    "@inngest/await-inngest-send": "warn",
    "@inngest/deterministic-sleep-until-dates": "warn",
    "@inngest/no-nested-steps": "error",
    "@inngest/no-variable-mutation-in-step": "error",
  },
};
