{
  "name": "trip-booker-bun-api",
  "version": "1.0.0",
  "private": true,
  "description": "Bun API for trip booking with Inngest durable workflows",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "lint": "bun run tsc --noEmit"
  },
  "dependencies": {
    "inngest": "^3.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.2"
  }
}
